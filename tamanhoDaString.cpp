#include <iostream>
#include <string>

int main() {
    std::string str = "Hello, world!";
    printf("Tamanho da string: %zu\n", str.size());
    return 0;
}

//alias enviaGit="git add . && git commit -m 'a' && git push"